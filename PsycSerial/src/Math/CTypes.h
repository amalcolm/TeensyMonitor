#pragma once
#pragma managed(push, off)

// common includes
#include <array>
#include <optional>
#include <cmath>

// useful type aliases
using Vec3 = std::array<double, 3>;
using Mat3 = std::array<std::array<double, 3>, 3>;
