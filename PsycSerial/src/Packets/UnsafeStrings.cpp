#include "UnsafeStrings.h"
/*
namespace PsycSerial
{
	void UnsafeString::SetTemplate(IWebMessage^ message)
	{
		String^ json = JsonSerializer::Serialize(message, options);

		Type^ type = message->GetType();

		if (typeToTemplate->ContainsKey(type))
			typeToTemplate[type] = json;
		else
			typeToTemplate->Add(type, json);
	}
}
*/