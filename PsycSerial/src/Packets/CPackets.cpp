#include "CPackets.h"
#pragma managed(push, off)

// Nothing required here right now; the header provides inline/constexpr helpers.
// This TU exists so you can toggle /clr- and keep native linkage boundaries neat.

#pragma managed(pop)
